
        {/* {[1, 2, 3, 4].map((i) => (
          <Link to="/template">
            <TemplateCard key={i} />
          </Link>
        ))} */}
        {console.log(details)}
        {details?.map((data, i) => {
          {
            <Link to="/template">
              <TemplateCard key={i} data={data} />
            </Link>;
          }
        })}

        {/* {templateList?.map((data, idx) => {
          <TemplateCard key={idx} data={data} />;
        })} */}